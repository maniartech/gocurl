// Package tests holds black-box tests that exercise the public gocurl API and
// the gocurl CLI exactly as an external consumer would — across HTTP methods,
// flags, and options. All tests are hermetic (httptest), never the live network.
package tests
